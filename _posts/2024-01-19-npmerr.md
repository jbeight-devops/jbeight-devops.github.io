

> vuepress로 된 소스를 빌드 하다가 마주한 고생들 

## npm install vs npm install -g
무슨 차이지?

## npm install vs npm ci
package-lock.jsons

## npm -v
어떤 폴더에서는 v16



## package.json
dependencies vs devDependencies

## ERR_REQUIRE_ESM

```angular2html
Err [ERR_REQUIRE_ESM]: require() of ES Module ~
```

Cannot use import statement outside a module Error

package.json type:module 설정 추가

err_require_esm require() of ES Module
